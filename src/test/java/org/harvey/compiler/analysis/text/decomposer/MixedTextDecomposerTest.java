package org.harvey.compiler.analysis.text.decomposer;

import org.junit.Test;

public class MixedTextDecomposerTest {

    @Test
    public void decompose() {
        /*MixedTextDecomposer text = new MixedTextDecomposer();
        SourceTextContext decomposed = text.decompose(
                new SourceString(SourceType.MIXED,
                        "12.1++--+23.1-+.-+.23a.a.a441-23..123",
                        new SourcePosition(2, 3)));
        decomposed.forEach(s-> System.out.print(s.getValue()+" "));*/
    }
}